function Coffee() {
    const hot = false; // Define the variable

    return (
        <h3>
            {hot ? "It is black coffee" : "It is cold coffee"}
        </h3>
    );
}

export default Coffee;
